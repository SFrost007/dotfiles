#!/bin/sh

PACKAGE_MANAGER_TITLE="Ruby Gems"
PACKAGE_MANAGER_INSTALL_CMD="gem install"
PACKAGE_MANAGER_NEEDS_SUDO=1

PACKAGES=(
  cocoapods
  pygmentize
  twterm
  xcodeproj
  xcpretty
)

PACKAGES_HOME=(
)

PACKAGES_WORK=(
)
